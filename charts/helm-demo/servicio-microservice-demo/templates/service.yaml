apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.environments }}
  name: {{ include "servicio-microservice-demo.fullname" . }}
  labels:
    {{- include "servicio-microservice-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servicio-microservice-demo.selectorLabels" . | nindent 4 }}

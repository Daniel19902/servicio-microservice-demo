apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "servicio-microservice-demo.fullname" . }}-test-connection"
  labels:
    {{- include "servicio-microservice-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "servicio-microservice-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

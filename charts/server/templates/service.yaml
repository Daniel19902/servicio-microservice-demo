apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.environments }}
  name: {{ .Chart.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 4530
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ .Chart.Name }}

